# Demo Assets

The README demo is generated from code:

- `validation-demo.tape` renders the terminal GIF with VHS.
- `render_validation_output.py` runs the representative and complex examples and prints their responses.
- `make demo` regenerates the README demo asset.
