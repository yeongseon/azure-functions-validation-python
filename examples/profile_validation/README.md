# Profile Validation Example

This example shows a more complete HTTP validation flow for the Azure Functions Python v2 programming model.

- Path validation via `users/{user_id}`
- Query validation via `verbose`
- Header validation via `x-request-id`
- Typed response serialization via `response_model`

