# Troubleshooting

## Common Issues

- **Import errors**: ensure `PYTHONPATH` includes `src/` (Makefile handles this).
- **Pydantic version conflicts**: this library requires Pydantic v2.
- **Azure Functions runtime issues**: out of scope (see SUPPORT.md).
